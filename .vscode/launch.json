{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "MSVC Debug",
            "type": "cppvsdbg",  // Windows下MSVC调试器
            "request": "launch",
            //"program": "${workspaceFolder}/build_msvc/test/${workspaceFolderBasename}.exe",
            "program": "${workspaceFolder}/bin/${workspaceFolderBasename}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/bin",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "Meson: install project"
        }
    ]
}